import { Link } from 'react-router-dom'
import { resources } from '../data/resources.js'

export default function Resources() {
  return <main className="resources-page"><section className="resources-hero-react"><div className="resources-bg-react" aria-hidden="true"><div className="resources-orb r1"/><div className="resources-orb r2"/><div className="resources-orb r3"/><div className="resources-grid"/></div><div className="container resources-hero-content"><p className="resources-badge">Free growth resources</p><h1>Useful tools for <span>better marketing decisions.</span></h1><p>Practical calculators, checklists and frameworks built from the work of running performance marketing campaigns and growth systems.</p><div className="resources-hero-actions"><Link className="resource-button primary" to="/resources/campaign-budget-calculator">Open campaign calculator <span>↗</span></Link><a className="resource-button secondary" href="#resource-library">Explore resources</a></div><div className="resource-chips"><span>Free to use</span><span>Practical</span><span>Built for marketers</span><span>UAE focused</span></div></div></section><section className="resources-library" id="resource-library"><div className="container"><div className="resources-heading-row"><div><p className="resources-eyebrow">Resource library</p><h2>Tools you can actually use.</h2></div><p>Designed to help you make a better decision before you spend more money, launch another campaign or change another funnel.</p></div><div className="resource-grid">{resources.map((resource) => <article className={`resource-card ${resource.featured ? 'featured' : ''}`} key={resource.id}><div className="resource-icon">{resource.icon}</div><div className="resource-type">{resource.type}</div><h3>{resource.title}</h3><p>{resource.description}</p>{resource.href.startsWith('/') ? <Link className="resource-card-link" to={resource.href}>Use this resource <span>→</span></Link> : <span className="resource-card-link muted">Coming next <span>→</span></span>}</article>)}</div></div></section></main>
}
