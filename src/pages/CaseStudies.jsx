import { Link } from 'react-router-dom'
import { caseStudies } from '../data/caseStudies.js'

export default function CaseStudies() {
  return <main className="case-studies-page"><section className="case-title-band"><div className="container case-center"><p className="case-eyebrow">Case Studies</p><h1>Case Studies</h1><p>Work measured in business impact.</p></div></section><section className="case-hero"><div className="case-grid-bg" aria-hidden="true"/><div className="container"><p className="case-eyebrow">Selected case studies</p><div className="case-hero-grid"><h2>Work measured in <span>business impact.</span></h2><p>A small selection of growth engagements across demand capture, conversion and lead management. Company details are kept private where needed.</p></div><div className="case-context"><div><small>The brief</small><p>Find the constraint that is limiting quality or scale.</p></div><div><small>The work</small><p>Connect media, message, experience and follow up.</p></div><div><small>The measure</small><p>Track signals that lead to commercial outcomes.</p></div></div></div></section><section className="case-list-section"><div className="container"><div className="case-grid">{caseStudies.map((study, index) => <article className="case-card surface" key={study.slug} style={{ '--delay': `${index * 60}ms` }}><div className={`case-visual case-tone-${index}`}><div className="case-visual-top"><span>{study.category}</span><small>{study.period}</small></div><div className="case-metrics">{study.metrics.map(([value, label]) => <div key={label}><small>{label}</small><strong>{value}</strong></div>)}</div></div><div className="case-card-body"><p className="case-category">{study.category} · UAE</p><h2>{study.title}</h2><p>{study.summary}</p><div className="case-focus"><span>Focus:</span> {study.focus}</div>{study.slug === 'crm-sales-qualified-lead' ? <Link to={`/case-studies/${study.slug}`}>Read case study ↗</Link> : <span className="case-private">Selected work</span>}</div></article>)}</div></div></section></main>
}
